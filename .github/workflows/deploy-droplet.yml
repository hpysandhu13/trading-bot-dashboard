name: Deploy to DigitalOcean Droplet

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: droplet-deploy-main
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Prepare SSH key
        shell: bash
        run: |
          set -euo pipefail
          install -m 700 -d ~/.ssh
          printf '%s' "${{ secrets.DROPLET_SSH_KEY_B64 }}" | tr -d '\r\n' | base64 -d | tr -d '\r' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keygen -y -f ~/.ssh/deploy_key > /dev/null
          host="$(printf '%s' "${{ secrets.DROPLET_HOST }}" | tr -d '\r\n')"
          ssh-keyscan -p 22 "$host" >> ~/.ssh/known_hosts

      - name: Deploy over SSH
        shell: bash
        run: |
          set -euo pipefail
          host="$(printf '%s' "${{ secrets.DROPLET_HOST }}" | tr -d '\r\n')"
          user="$(printf '%s' "${{ secrets.DROPLET_USER }}" | tr -d '\r\n')"
          ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes -p 22 "$user@$host" '
            set -e
            cd /var/www/trading-bot-dashboard
            git pull --ff-only origin main
            systemctl reload nginx
            systemctl is-active nginx
          '
